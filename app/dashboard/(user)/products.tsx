import React from "react";
import { Text, View } from "react-native";

const products = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-lg font-semibold">Daftar Produk</Text>
    </View>
  );
};

export default products;
